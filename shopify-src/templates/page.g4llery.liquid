{% layout 'theme' %}

<div class="page-width">
  <div class="grid">
    <div class="grid__item">
      <div class="rte">
        {{ page.content }}
      </div>
    </div>
  </div>
</div>
{% layout 'theme' %}
{% section 'gallery-slideshow' %}
